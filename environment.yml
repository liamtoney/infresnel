name: infresnel
channels:
  - conda-forge
  - defaults
dependencies:
  - colorcet
  - ipywidgets
  - matplotlib
  - notebook
  - numba
  - pip
  - pygmt
  - rioxarray
  - tqdm
  - pip:
    - --editable .
