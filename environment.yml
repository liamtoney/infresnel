name: infresnel
channels:
  - conda-forge
  - defaults
dependencies:
#  - matplotlib  # OPTIONAL, for plotting
  - colorcet
  - notebook
  - numba
  - pip
  - pygmt
  - rioxarray
  - pip:
    - --editable .
